public class Main {

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5, 6};
        int target = 4;
        printTargetCombinations(arr, target);
    }

    // prints all combinations of arr that, when summed, equal target
    // it is assumed there are no repeat values in arr
    private static void printTargetCombinations(int[] arr, int target) {

        // this outer loop iterates over all possible combination sizes
        for (int i = 0; i < arr.length; i++) {

        }
    }

    // Returns a 2D array of all combinations of arr of size r.
    private static int[][] getCombinations(int[] arr, int r) {
        int [][] result = new int[10][10];
        return result;
    }
}
